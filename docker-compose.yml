services:
  svcdesk:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      SVCDESK_TEST_CLOCK: "1"
      DB_PATH: /data/svcdesk.db
    volumes:
      - svcdesk_data:/data

volumes:
  svcdesk_data:
