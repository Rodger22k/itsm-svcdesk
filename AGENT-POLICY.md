# Agent policy

- Bash(rm *): deleting repository files has an irreversible blast radius and remains the repository owner's decision.
- Bash(git push *): publishing a commit changes the shared GitHub history and must be approved by the repository owner.
- Bash(docker *): Docker commands can stop, remove or rebuild containers outside the narrow review task and are not allowed.
